# Task: UI/アニメーション全面改善

Quest App の UI/UX を改善する。以下のファイルを編集すること。

## 禁止事項
- console.log を残さない
- dangerouslySetInnerHTML を使わない
- 新しい npm パッケージを追加しない
- middleware.ts を変更しない

---

## 1. `src/app/globals.css` に以下を追記（既存アニメーションは変更しない）

```css
/* Page fade-in */
@keyframes page-fade-in {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}

.page-enter {
  animation: page-fade-in 0.3s ease-out;
}

/* Card hover glow */
@keyframes card-glow-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(99, 102, 241, 0); }
  50% { box-shadow: 0 0 20px 2px rgba(99, 102, 241, 0.15); }
}

.card-glow:hover {
  animation: card-glow-pulse 2s ease-in-out infinite;
}

/* XP bar gradient */
@keyframes xp-gradient-shift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.xp-bar-gradient {
  background: linear-gradient(90deg, #6366f1, #818cf8, #a78bfa, #818cf8, #6366f1);
  background-size: 200% 100%;
  animation: xp-gradient-shift 3s ease-in-out infinite;
}

/* Stagger children */
@keyframes stagger-fade-in {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}

.stagger-children > * {
  animation: stagger-fade-in 0.4s ease-out both;
}

.stagger-children > *:nth-child(1) { animation-delay: 0ms; }
.stagger-children > *:nth-child(2) { animation-delay: 60ms; }
.stagger-children > *:nth-child(3) { animation-delay: 120ms; }
.stagger-children > *:nth-child(4) { animation-delay: 180ms; }
.stagger-children > *:nth-child(5) { animation-delay: 240ms; }
.stagger-children > *:nth-child(6) { animation-delay: 300ms; }
.stagger-children > *:nth-child(7) { animation-delay: 360ms; }
.stagger-children > *:nth-child(8) { animation-delay: 420ms; }
.stagger-children > *:nth-child(9) { animation-delay: 480ms; }
.stagger-children > *:nth-child(10) { animation-delay: 540ms; }

/* Skeleton shimmer */
@keyframes skeleton-shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.skeleton {
  background: linear-gradient(90deg, var(--muted-bg) 25%, var(--card-border) 50%, var(--muted-bg) 75%);
  background-size: 200% 100%;
  animation: skeleton-shimmer 1.5s ease-in-out infinite;
  border-radius: 0.5rem;
}

/* Count pop */
@keyframes count-pop {
  0% { transform: scale(1); }
  50% { transform: scale(1.3); }
  100% { transform: scale(1); }
}

.count-pop {
  animation: count-pop 0.3s ease-out;
}

/* Glow effects */
.glow-primary {
  box-shadow: 0 0 12px 2px rgba(99, 102, 241, 0.3);
}

.glow-success {
  box-shadow: 0 0 12px 2px rgba(16, 185, 129, 0.3);
}

.glow-accent {
  box-shadow: 0 0 12px 2px rgba(245, 158, 11, 0.3);
}

/* Result output styling */
.result-output {
  line-height: 1.7;
  letter-spacing: 0.01em;
}

/* Gentle float */
@keyframes float-gentle {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-4px); }
}

.float-gentle {
  animation: float-gentle 3s ease-in-out infinite;
}
```

## 2. 新規: `src/components/ui/NavLink.tsx`

```tsx
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { cn } from '@/lib/utils/cn';

interface NavLinkProps {
  href: string;
  icon: React.ReactNode;
  label: string;
}

export function NavLink({ href, icon, label }: NavLinkProps) {
  const pathname = usePathname();
  const isActive = href === '/' ? pathname === '/' : pathname.startsWith(href);

  return (
    <Link
      href={href}
      className={cn(
        'flex items-center gap-1 rounded-md px-2 py-1 text-sm transition-colors',
        isActive
          ? 'bg-quest-primary/10 text-quest-primary font-medium'
          : 'text-muted hover:text-foreground hover:bg-muted-bg'
      )}
    >
      {icon}
      <span className="hidden sm:inline">{label}</span>
    </Link>
  );
}
```

## 3. `src/app/(main)/layout.tsx` — NavLink使用

3つのLink（history, adventure, setup）を NavLink に置き換え。
`import { NavLink } from '@/components/ui/NavLink';` を追加。

## 4. `src/components/ui/XpProgressBar.tsx` — グラデーションXPバー

levelのisMax判定を `level >= 10` に変更。
XPバーの内側divに `xp-bar-gradient` クラスを適用（bg-quest-primary を置換）。
bar heightを h-2 → h-2.5 に。
XP値表示を `font-medium text-foreground` に。

## 5. `src/features/quest/components/QuestCard.tsx` — ホバー強化

cn import追加。Cardの className を:
```
cn(
  'relative h-full transition-all duration-200',
  locked
    ? 'opacity-50'
    : 'card-glow hover:shadow-lg hover:-translate-y-0.5 hover:border-quest-primary/40'
)
```

## 6. `src/features/quest/components/QuestExecution.tsx` — ローディング改善

全面書き替え（上記の詳細仕様参照）。import追加: useState, Loader2。
マスコットメッセージローテーション、ドットアニメーション、パルスドット追加。

## 7. `src/features/quest/components/QuestResult.tsx` — 結果スタイリング

- 全体を `page-enter` クラスで包む
- pre要素を `result-output` クラス付きdivに変更
- XPバッジに `glow-success` 追加、font-bold化
- padding p-4 → p-5

## 8. `src/features/quest/components/HistoryList.tsx` — 空状態改善

Sparkles import追加。空状態にアイコン（float-gentle）、ボーダーdashed化、CTA追加。

## 9. `src/components/ui/Modal.tsx` — backdrop-blur

overlay の `bg-black/50` に `backdrop-blur-sm` 追加。

## 10. `src/features/gamification/components/LevelUpModal.tsx` — 演出強化

レベル数字のdivに `glow-primary` 追加、spanに `count-pop` 追加。

## 11. `src/features/adventure/components/ChapterCard.tsx` — ホバー改善

Cardの className を transition-all duration-200 に変更、hover効果強化、currentに glow-primary。

## 12. `src/app/page.tsx` — ランディング + ホーム改善

- FeatureCard: hover:shadow-lg hover:-translate-y-1 hover:border-quest-primary/30 追加
- ヒーローh1: インラインスタイルでグラデーション
- Feature cards grid: stagger-children 追加
- クエストgrid: stagger-children 追加
- 冒険カード: hover:shadow-lg hover:-translate-y-0.5 追加

## ビルド確認

```bash
cd /Users/naokijodan/Desktop/quest-app && npm run build
```
