# Task: Sprint9-Offline-Banner
# AI: Codex CLI
# Priority: Medium
# Risk Level: Low

## 1. Objective（目的）
PWAのオフライン状態を検知して表示するバナーコンポーネントを作成する。

## 2. Target Files（対象ファイル）
### Create（新規作成）
- src/hooks/useOnlineStatus.ts — navigator.onLine + online/offline イベントのカスタムフック
- src/components/ui/OfflineBanner.tsx — オフライン時に表示するバナー

### Modify（変更）
- src/app/(main)/layout.tsx — OfflineBannerを追加

## 3. Dependencies（依存ファイル — 必ず読むこと）
- src/app/(main)/layout.tsx（現在のメインレイアウト）
- src/features/quest/components/AgentStatusBanner.tsx（参考: ステータスバナーの実装パターン）

## 4. Requirements（仕様）

### src/hooks/useOnlineStatus.ts
```typescript
'use client';
import { useSyncExternalStore } from 'react';

function subscribe(callback: () => void) {
  window.addEventListener('online', callback);
  window.addEventListener('offline', callback);
  return () => {
    window.removeEventListener('online', callback);
    window.removeEventListener('offline', callback);
  };
}

function getSnapshot() {
  return navigator.onLine;
}

function getServerSnapshot() {
  return true; // SSR時はオンライン扱い
}

export function useOnlineStatus(): boolean {
  return useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
}
```

### src/components/ui/OfflineBanner.tsx
```typescript
'use client';
import { useOnlineStatus } from '@/hooks/useOnlineStatus';
import { WifiOff } from 'lucide-react';

export function OfflineBanner() {
  const isOnline = useOnlineStatus();
  if (isOnline) return null;

  return (
    <div className="flex items-center gap-2 bg-quest-accent/20 px-4 py-2 text-sm text-quest-accent">
      <WifiOff className="h-4 w-4" />
      <span>オフライン状態です。一部の機能が制限されます。</span>
    </div>
  );
}
```

### src/app/(main)/layout.tsx の変更
- `<OfflineBanner />` をメインコンテンツの上に追加
- import { OfflineBanner } from '@/components/ui/OfflineBanner';

## 5. Constraints（制約）
- TailwindCSS 4のみ使用
- lucide-reactのアイコンを使用（既存依存）
- useSyncExternalStoreを使う（React 19対応）
- ファイルは50行以内

## 6. Forbidden（禁止事項）
- [ ] console.log の残置
- [ ] 外部ライブラリの追加
- [ ] dangerouslySetInnerHTML

## 7. Acceptance Criteria
- [ ] TypeScript型エラーなし
- [ ] オフライン時にバナーが表示される
- [ ] オンラインに戻ったらバナーが消える
- [ ] SSR時にエラーが出ない
