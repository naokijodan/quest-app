# Task: Phase3-BossBattle-Victory
# AI: Codex CLI
# Priority: Medium
# Risk Level: Low

## 1. Objective
Create a boss battle victory celebration component for the final quest (Chapter 5, adv-5-2). When the user completes the last adventure quest, show a special full-screen victory animation with the "ターミナルマスター" title.

## 2. Target Files
### Create
- `src/features/adventure/components/BossVictory.tsx` — Full-screen victory animation
- `src/features/adventure/components/AdventureChapterComplete.tsx` — Chapter completion summary

### Modify
- `src/features/adventure/components/AdventureQuestRunner.tsx` — Show BossVictory for the final quest

## 3. Dependencies (must read first)
- `src/features/adventure/components/AdventureQuestRunner.tsx` — Current quest runner
- `src/features/adventure/types/index.ts` — CHAPTERS constant
- `src/components/ui/Button.tsx` — Button component
- `src/components/ui/Modal.tsx` — Modal component
- `src/features/gamification/components/LevelUpModal.tsx` — Existing level-up modal pattern

## 4. Requirements

### 4.1 src/features/adventure/components/BossVictory.tsx
Full-screen victory celebration:
```typescript
'use client';
interface BossVictoryProps {
  open: boolean;
  onClose: () => void;
  username: string;
}
```

Features:
- Full-screen overlay with dark background and animation
- Big center text: "ターミナルマスター" with glow effect
- Subtext: "{username}はターミナルを克服した！"
- Confetti/sparkle animation using CSS keyframes (no external deps)
- "冒険の記録を見る" button that links to /history
- "ホームに戻る" button
- Auto-play entrance animation

CSS animations to use (inline Tailwind + keyframes in the component):
- Text fade-in + scale-up
- Sparkle particles (floating dots)
- Background pulse

### 4.2 src/features/adventure/components/AdventureChapterComplete.tsx
Chapter completion card:
```typescript
interface Props {
  chapter: number;
  title: string;
  nextChapter?: { number: number; title: string } | null;
}
```
- Shows "第{chapter}章クリア！" with chapter title
- If nextChapter exists, shows "次の章: {title}" with link
- If no next chapter (final), shows special congratulations

### 4.3 AdventureQuestRunner modification
After completing quest adv-5-2 (identified by `quest.quest_identifier === 'adv-5-2'`), show `BossVictory` instead of the regular `StoryDialog` complete dialog.

Add detection:
```typescript
const isFinalBoss = quest.quest_identifier === 'adv-5-2';
```

In the phase === 'completed' effect:
- If isFinalBoss: show BossVictory
- Otherwise: show StoryDialog as before

## 5. Constraints
- CSS-only animations (no external animation libraries)
- Use existing Tailwind tokens where possible
- Japanese text only
- No console.log
- No new dependencies
- Immutable patterns

## 6. Forbidden
- [ ] External animation libraries (framer-motion, etc.)
- [ ] console.log
- [ ] New npm packages

## 7. Acceptance Criteria
- [ ] BossVictory.tsx renders with full-screen animation
- [ ] adv-5-2 completion triggers BossVictory
- [ ] Other quests use StoryDialog as before
- [ ] AdventureChapterComplete shows chapter summary
- [ ] TypeScript compiles without errors
